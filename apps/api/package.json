{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply quest-maker-v3 --local",
    "db:migrate:remote": "wrangler d1 migrations apply quest-maker-v3 --remote",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@dossier/core": "workspace:*",
    "hono": "^4.12.0",
    "@hono/zod-validator": "^0.5.0",
    "drizzle-orm": "^0.45.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.14.0",
    "drizzle-kit": "^0.31.0",
    "vitest": "^3.2.3",
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20250530.0"
  }
}
