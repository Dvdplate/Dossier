# Local browser dev — Vite proxies /api to the Worker on :8787
VITE_API_BASE_URL=/api

# Production / Android builds use .env.production with an absolute Worker URL:
# VITE_API_BASE_URL=https://dossier.<account-subdomain>.workers.dev/api
